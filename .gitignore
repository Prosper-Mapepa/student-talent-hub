# OS
.DS_Store
*.pem

# Environment (each app has its own .env.example)
.env
.env.*
!.env.example

# Dependencies — each app manages its own node_modules
node_modules/

# Next.js (frontend lives in ./frontend)
.next/
out/

# Expo / React Native (mobile lives in ./mobile)
mobile/.expo/
mobile/dist/
mobile/web-build/

# Build artifacts
dist/
build/
coverage/
*.tsbuildinfo

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# IDE / tooling
.vercel
.pnp
.pnp.*
